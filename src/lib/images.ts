/**
 * Image sources.
 *
 * All photography is Decodreams' own work and lives inside the repository at
 * /public/images/projects, so the site is fully self-contained: nothing here
 * depends on an external host after a GitHub → Vercel deploy.
 *
 * Paths are absolute public paths ("/images/..."), which is what next/image
 * needs. Filenames are lowercase with hyphens — Linux is case-sensitive.
 */

/** Full-bleed hero. Warm evening light, layered lighting, low seating. */
export const heroImage = "/images/projects/luxury-living-room-01.jpg";

/** Portrait detail shot beside the studio story. */
export const studioImage = "/images/projects/interior-detail-01.jpg";
